# nightly-deep.yml v1
# Nightly deep tests with local server on main and dev branches.
name: Nightly Deep Tests

on:
  workflow_dispatch:
  schedule:
    # Every night at 03:00 Rome time (02:00 UTC during CET)
    # Runs 30 min after ci-real.yml to avoid overlap
    - cron: "0 2 * * *"

permissions:
  issues: write
  contents: read

jobs:
  test_deep:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        branch: [main, dev]
        include:
          - branch: main
            issue_label: "nightly-deep-main"
            issue_label_color: "d93f0b"
          - branch: dev
            issue_label: "nightly-deep-dev"
            issue_label_color: "e99695"

    env:
      MODE: MOCK
      STRICT_NETWORK: "0"
      FRONTEND_ORIGIN: "http://localhost:4173"
      ADA_TEST_PASSWORD: ${{ secrets.ADA_TEST_PASSWORD }}
      ADA_TEST_EMAIL: ${{ secrets.TEST_VET_EMAIL }}
      TEST_SUPER_ADMIN_EMAIL: ${{ secrets.TEST_SUPER_ADMIN_EMAIL }}
      TEST_ADMIN_BRAND_EMAIL: ${{ secrets.TEST_ADMIN_BRAND_EMAIL }}
      TEST_VET_EMAIL: ${{ secrets.TEST_VET_EMAIL }}
      TEST_OWNER_EMAIL: ${{ secrets.TEST_OWNER_EMAIL }}
      TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}
      DEPLOY_URL: ${{ secrets.DEPLOY_URL }}
      RATE_LIMIT_PER_MIN: "600"
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ matrix.branch }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install backend dependencies
        run: npm install --prefix backend

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-ms-playwright-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Start server
        run: |
          nohup node backend/src/server.js > backend.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:3000/api/health > /dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:3000/api/health > /dev/null
          nohup npm run serve > server.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:4173 > /dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4173 > /dev/null

      - name: Seed test users
        run: node backend/src/seed.test-users.js

      - name: Nightly deep tests
        id: tests
        run: npm run test:ci:nightly
        continue-on-error: true

      # ── Artifacts (always, for debugging) ──
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-deep-${{ matrix.branch }}
          path: playwright-report/
          if-no-files-found: ignore

      - name: Upload test-results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-deep-${{ matrix.branch }}
          path: test-results/
          if-no-files-found: ignore

      - name: Upload server log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: server-log-deep-${{ matrix.branch }}
          path: server.log
          if-no-files-found: ignore

      - name: Upload backend log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-log-deep-${{ matrix.branch }}
          path: backend.log
          if-no-files-found: ignore

      # ── Issue management ──
      - name: Open/update failure issue
        if: steps.tests.outcome == 'failure'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          DATE=$(date -u +"%Y-%m-%d %H:%M UTC")
          LABEL="${{ matrix.issue_label }}"

          gh label create "$LABEL" --color "${{ matrix.issue_label_color }}" \
            --description "Nightly deep (${{ matrix.branch }}) failure" 2>/dev/null || true

          ISSUE_NUM=$(gh issue list --label "$LABEL" --state open --json number --jq '.[0].number // empty')

          if [ -z "$ISSUE_NUM" ]; then
            gh issue create \
              --title "Nightly deep (${{ matrix.branch }}) failed — $DATE" \
              --label "$LABEL" \
              --body "## Nightly deep tests failed

          - **Date:** $DATE
          - **Branch:** ${{ matrix.branch }}
          - **Run:** $RUN_URL
          - **Trigger:** ${{ github.event_name }}

          ### Next steps
          1. Check the [run logs]($RUN_URL) and download artifacts
          2. Fix the failing test(s)
          3. Close this issue when resolved"
          else
            gh issue comment "$ISSUE_NUM" \
              --body "### Still failing — $DATE
          - **Run:** $RUN_URL
          - **Trigger:** ${{ github.event_name }}"
          fi

      - name: Close failure issue (on success)
        if: steps.tests.outcome == 'success'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          LABEL="${{ matrix.issue_label }}"
          ISSUE_NUM=$(gh issue list --label "$LABEL" --state open --json number --jq '.[0].number // empty')
          if [ -n "$ISSUE_NUM" ]; then
            gh issue close "$ISSUE_NUM" --comment "Nightly deep (${{ matrix.branch }}) passed — auto-closing."
          fi

      # ── Telegram notification on failure ──
      - name: Notify Telegram (failure)
        if: steps.tests.outcome == 'failure'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          MSG="❌ *Nightly deep (${{ matrix.branch }}) FAILED*
          📅 $(date -u +'%Y-%m-%d %H:%M UTC')
          🔗 [View run]($RUN_URL)"

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview="true" \
            -d text="$MSG"

      # ── Summary ──
      - name: Write CI summary
        if: always()
        run: |
          {
            echo "## Nightly Deep Tests summary"
            echo ""
            echo "| Field | Value |"
            echo "|-------|-------|"
            echo "| Branch | \`${{ matrix.branch }}\` |"
            echo "| Result | \`${{ steps.tests.outcome }}\` |"
            echo "| MODE | MOCK (local server) |"
            echo ""
            echo "### Artifacts"
            echo "- playwright-report-deep-${{ matrix.branch }}"
            echo "- test-results-deep-${{ matrix.branch }}"
            echo "- server-log-deep-${{ matrix.branch }}"
            echo "- backend-log-deep-${{ matrix.branch }}"
          } >> $GITHUB_STEP_SUMMARY

      # ── Fail the job if tests failed ──
      - name: Fail if tests failed
        if: steps.tests.outcome == 'failure'
        run: exit 1
