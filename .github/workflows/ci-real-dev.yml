# ci-real-dev.yml v1
# Nightly CI (REAL) for the dev branch.
# Mirrors ci-real.yml but checks out `dev` code and tests only against the dev environment.
# NOTE: GitHub schedules only fire on the default branch (main), so this file
#       must be present on main for the cron trigger to work.

name: CI (REAL — dev branch)

on:
  workflow_dispatch:
  schedule:
    # Nightly at 03:30 Rome time (02:30 UTC during CET) — 1 hour after the prod nightly
    - cron: "30 2 * * *"

permissions:
  issues: write

jobs:
  test_real_dev:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      MODE: REAL
      STRICT_NETWORK: "1"
      STRICT_ALLOW_HOSTS: "cdnjs.cloudflare.com"
      ALLOW_OPENAI: "1"
      FRONTEND_ORIGIN: "http://localhost:4173"
      ADA_ENV: dev
      ADA_TEST_PASSWORD: ${{ secrets.ADA_TEST_PASSWORD }}
      ADA_TEST_EMAIL: ${{ secrets.TEST_VET_EMAIL }}
      TEST_SUPER_ADMIN_EMAIL: ${{ secrets.TEST_SUPER_ADMIN_EMAIL }}
      TEST_ADMIN_BRAND_EMAIL: ${{ secrets.TEST_ADMIN_BRAND_EMAIL }}
      TEST_VET_EMAIL: ${{ secrets.TEST_VET_EMAIL }}
      TEST_OWNER_EMAIL: ${{ secrets.TEST_OWNER_EMAIL }}
      TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      DEPLOY_URL: ${{ secrets.DEPLOY_URL_DEV }}
      RATE_LIMIT_PER_MIN: "600"
      DATABASE_URL: ${{ secrets.DATABASE_URL_DEV }}

    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v4
        with:
          ref: dev

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install backend dependencies
        run: npm install --prefix backend

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-ms-playwright-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Start server
        run: |
          nohup node backend/src/server.js > backend.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:3000/api/health > /dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:3000/api/health > /dev/null
          nohup npm run serve > server.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:4173 > /dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4173 > /dev/null

      - name: Seed test users
        run: node backend/src/seed.test-users.js

      - name: CI tests (REAL)
        run: npm run test:ci:real

      - name: Upload Playwright report (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-dev
          path: playwright-report/
          if-no-files-found: ignore

      - name: Upload test-results (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-dev
          path: test-results/
          if-no-files-found: ignore

      - name: Upload server log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: server-log-dev
          path: server.log
          if-no-files-found: ignore

      - name: Upload backend log (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: backend-log-dev
          path: backend.log
          if-no-files-found: ignore

      - name: Notify nightly failure (open/update issue)
        if: failure()
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          DATE=$(date -u +"%Y-%m-%d %H:%M UTC")
          LABEL="nightly-failure-dev"

          # Ensure label exists
          gh label create "$LABEL" --color "e99695" --description "Nightly CI (REAL) dev branch failure" 2>/dev/null || true

          # Check for existing open issue
          ISSUE_NUM=$(gh issue list --label "$LABEL" --state open --json number --jq '.[0].number // empty')

          if [ -z "$ISSUE_NUM" ]; then
            # Create new issue
            gh issue create \
              --title "Nightly CI (REAL/dev) failed — $DATE" \
              --label "$LABEL" \
              --body "$(cat <<EOF
          ## Nightly CI (REAL) — dev branch failed

          - **Date:** $DATE
          - **Run:** $RUN_URL
          - **Branch:** dev
          - **Trigger:** ${{ github.event_name }}

          ### Next steps
          1. Check the [run logs]($RUN_URL) and download artifacts (playwright-report, test-results)
          2. Fix the failing test(s)
          3. Close this issue when resolved
          EOF
          )"
          else
            # Comment on existing issue
            gh issue comment "$ISSUE_NUM" \
              --body "### Still failing — $DATE
          - **Run:** $RUN_URL
          - **Trigger:** ${{ github.event_name }}"
          fi

      - name: Close nightly failure issue (on success)
        if: success()
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          LABEL="nightly-failure-dev"
          ISSUE_NUM=$(gh issue list --label "$LABEL" --state open --json number --jq '.[0].number // empty')
          if [ -n "$ISSUE_NUM" ]; then
            gh issue close "$ISSUE_NUM" --comment "Nightly CI (REAL/dev) passed — auto-closing."
          fi

      - name: Write CI summary
        if: always()
        run: |
          {
            echo "## CI (REAL — dev branch) summary"
            echo ""
            echo "- MODE: ${MODE}"
            echo "- STRICT_NETWORK: ${STRICT_NETWORK}"
            echo "- STRICT_ALLOW_HOSTS: ${STRICT_ALLOW_HOSTS:-}"
            echo "- ALLOW_OPENAI: ${ALLOW_OPENAI:-}"
            echo "- Branch: dev"
            echo ""
            echo "### Artifacts (available on failure)"
            echo "- playwright-report-dev"
            echo "- test-results-dev"
            echo "- server-log-dev"
            echo ""
            echo "### Useful commands (local)"
            echo "- npm ci"
            echo "- npm run serve  # port 4173"
            echo "- npm run test:ci:real"
          } >> $GITHUB_STEP_SUMMARY
